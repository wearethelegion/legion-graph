# ============================================================
# kgrag-rest-api extras (installed ON TOP of kgrag-base)
# Packages used ONLY by the REST API image.
# ============================================================

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9

# ── Chonkie semantic chunking with Gemini embeddings ─────────
# The [semantic,gemini] variant is only used by the REST API chunking logic.
# Pinned to 1.5.2 to prevent pip resolution-too-deep with google-* transitive deps.
chonkie[semantic,gemini]==1.5.2

# ── TOTP / QR ────────────────────────────────────────────────
pyotp>=2.9.0
qrcode[pil]>=7.4

# ── Alembic migrations (run at startup by rest-api) ──────────
alembic>=1.13.0

# ── OAuth2 / email / external integrations ───────────────────
authlib>=1.3.0
infobip-api-python-client==6.0.0

# ── FastMCP (if needed by REST API endpoints) ─────────────────
fastmcp==2.13.0.2

# ── Memory store (if used by any kept API route) ─────────────
mem0ai==1.0.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
psutil>=5.9.0
